indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /home/studentp/code/bonsai/log/searchd.log
  query_log = /home/studentp/code/bonsai/log/searchd.query.log
  pid_file = /home/studentp/code/bonsai/log/searchd.development.pid
}

source page_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = bonsai
  sql_pass = bonsai
  sql_db = bonsai_development
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `pages`.`id` * 1 + 0 AS `id` , CAST(GROUP_CONCAT(DISTINCT `page_part_revisions`.`body` SEPARATOR ' ') AS CHAR) AS `page_part`, `pages`.`id` AS `sphinx_internal_id`, 3023575326 AS `class_crc`, '1973180616,3023575326' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM pages    LEFT OUTER JOIN `page_parts` ON (`pages`.`id` = `page_parts`.`page_id`)  LEFT OUTER JOIN `page_part_revisions` ON (`page_part_revisions`.`page_part_id` = `page_parts`.`id`)  WHERE `pages`.`id` >= $start   AND `pages`.`id` <= $end    GROUP BY `pages`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `pages` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `pages` WHERE  `id` = (($id - 0) / 1)
}

index page_core
{
  source = page_core_0
  path = /home/studentp/code/bonsai/db/sphinx/development/page_core
  morphology = stem_en
  charset_type = utf-8
}

index page
{
  type = distributed
  local = page_core
}
